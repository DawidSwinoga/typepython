test = [1,2,3,1,2,3,4]

def mul(element: int): int:
    return element * element

def printv(vec: list<int>):
    for el in vec:
        print(el)

print("before:")
printv(test)
print("after:")
printv(map(test, mul))
