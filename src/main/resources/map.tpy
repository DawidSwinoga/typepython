collection = [1,2,3,1,2,3,4]

def printc(collection: list<int>):
    for element in collection:
        print(element)

def mul(element: int): int:
    return element * element

println("Przed wywołaniem funkcji map:")
printc(collection)
println("\nPo wywołaniu funkc:")
printc(map(collection, mul))
